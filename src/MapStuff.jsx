import { Text, View } from 'react-native'
import React, { Component } from 'react'
 
export const empty = [ //crea el array bidireccional para el posicionamiento en el mapa
    ['', '', '',],
    ['', '', '',],
    ['', '', '',],
];

